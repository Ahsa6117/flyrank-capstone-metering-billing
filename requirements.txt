# Floor-pinned: exact pins are avoided because this project targets Python 3.14,
# where only recent releases ship prebuilt wheels. Resolved versions are recorded
# in requirements.lock.txt for reproducibility.
fastapi>=0.116
uvicorn>=0.35
sqlalchemy>=2.0.36
pydantic>=2.11
pydantic-settings>=2.7
python-dotenv>=1.0.1
stripe>=11.4
apscheduler>=3.11
httpx>=0.28
pytest>=8.3
